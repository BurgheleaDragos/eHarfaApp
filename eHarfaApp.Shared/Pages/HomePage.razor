@page "/"
@using eHarfaApp.Shared.Services
@inject IFormFactor FormFactor

<section>
    <PageTitle>@PageTitle</PageTitle>
    <SectionContent SectionName="page-title" >@PageTitle</SectionContent>

    <MudContainer >
        <MudTextField
            T="string"
            bind-Value="Search"
            Label="Cautare"
            Adornment="Adornment.End"
            AdornmentIcon="@Icons.Material.Outlined.Search" />
        <MudSwipeArea OnSwipeEnd="HandleSwipe" Style="width: 100%;">
            <MudTabs
                Outlined="false"
                ApplyEffectsToContainer="false"
                Class="mt-8"
                TabPanelsClass="py-0"
                Style="height:75vh;"
                Border="false"
                Centered="true"
                MinimumTabWidth="30px"
                @bind-ActivePanelIndex="SelectedCategory"
                Position="Position.Bottom"
            >
                @foreach (var category in Categories)
                {
                    <MudTabPanel ID="@category.Id" Icon="@category.Icon" >
                        <MudContainer Style="max-height: calc(100vh - 300px);" Class="d-flex align-start overflow-x-auto">
                            <MudList T="string" SelectedValueChanged="SelectSong" Class="align-start">
                                @for (var i = 0; i < FilteredSongs.Count(); i++)
                                {
                                    <MudListItem 
                                        T="string" 
                                        Text=@($"{i}. {FilteredSongs[i].Title}") 
                                        Value="@FilteredSongs[i].Id" >
                                    </MudListItem>     
                                }
                            </MudList>
                        </MudContainer>
                    </MudTabPanel>
                }
            </MudTabs>
        </MudSwipeArea>
    </MudContainer>
</section>